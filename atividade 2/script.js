// Questão 1

var parametro = (numero, percentual) => {
    let calculo = (numero/100) * percentual;
    return calculo;
  }
// Questão 2

var arrays = (array1, array2) => {
    var juncao = array1.concat(array2);
    var str = juncao.toString();
    return str;
  }